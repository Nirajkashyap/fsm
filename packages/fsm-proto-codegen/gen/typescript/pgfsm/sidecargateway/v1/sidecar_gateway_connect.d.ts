// @generated by protoc-gen-connect-es v1.7.0
// @generated from file pgfsm/sidecargateway/v1/sidecar_gateway.proto (package pgfsm.sidecargateway.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

import { ConnectRequest, ConnectResponse } from "./sidecar_gateway_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * Worker-facing contract for the Activity Gateway's sidecar leg — the
 * register/invoke/heartbeat channel between the gateway and each language's
 * worker SDK, previously a hand-rolled length-prefixed-JSON envelope
 * (sidecar/protocol.ts) hand-ported per language by fsm-compiler-ts's
 * worker-sdk-protocol.eta templates with no schema to check them against
 * (#88). This proto is the single source of truth those copies drove from;
 * see #100 for the migration.
 *
 * Unlike activity-gateway.proto (the client-facing leg, #86/#87 — a client
 * calls Invoke() and gets one response back), this leg is not unary: the
 * worker initiates the connection and pushes Register once, then
 * Heartbeat/InvokeResult/InvokeError as needed, while the gateway pushes
 * Invoke/Cancel back unsolicited on the same stream — a worker registers
 * once and serves invocations indefinitely without the gateway needing to
 * reach it externally. A single bidi-streaming RPC preserves that shape.
 *
 * Unlike the new proto's file layout, activity-gateway.proto predates buf
 * adoption and is exempted from several STANDARD lint rules (see that
 * package's buf.yaml) as an already-shipped contract. This file has no such
 * history, so it follows FILE_LOWER_SNAKE_CASE, PACKAGE_DIRECTORY_MATCH,
 * PACKAGE_VERSION_SUFFIX, SERVICE_SUFFIX, and RPC_REQUEST_STANDARD_NAME from
 * the start.
 *
 * @generated from service pgfsm.sidecargateway.v1.SidecarGatewayService
 */
export declare const SidecarGatewayService: {
  readonly typeName: "pgfsm.sidecargateway.v1.SidecarGatewayService",
  readonly methods: {
    /**
     * @generated from rpc pgfsm.sidecargateway.v1.SidecarGatewayService.Connect
     */
    readonly connect: {
      readonly name: "Connect",
      readonly I: typeof ConnectRequest,
      readonly O: typeof ConnectResponse,
      readonly kind: MethodKind.BiDiStreaming,
    },
  }
};

